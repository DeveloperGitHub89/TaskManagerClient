import { Header } from "./Header";

export function Dashboard(){
    return (
        <>
            <Header alertText='Welcome to task manager app' description='You can create a task, view all tasks, and manage them' />
        </>
    );
}